{
  "header": {
    "eyebrow": "playground",
    "title": "Build a schema, then watch it become MySQL.",
    "lede1": "One continuous world. Define a model, write rows into it, stop the ",
    "lede2": " mid-flight and see the system be honestly incomplete — then let them finish and watch the identical query start returning rows.",
    "daemonLabel": "daemons",
    "reducedBefore": "Reduced motion is on, so the clock will not run on its own. Use ",
    "reducedAfter": " to advance it one tick at a time."
  },
  "simulationNotice": {
    "tag": "simulation",
    "before": "This page models the engine in the browser. It is not connected to a MySQL server, and no PHP runs anywhere — the rules it enforces were written by hand to match the engine's, and where the two ever disagree, ",
    "linkText": "the engine is right",
    "after": ". Slot layouts, statuses, chunk sizes and event names are the real ones; row counts are scaled down to what a browser can hold."
  },
  "sections": {
    "define": "the model builder",
    "tables": "the tables",
    "write": "the entry writer",
    "daemons": "the daemons",
    "query": "the query builder",
    "evolve": "the schema changer"
  },
  "modelBuilder": {
    "eyebrow": "section a",
    "title": "Define your models",
    "lede1": "Add fields, name them, decide which ones you will want to filter on. Your tenants do this at runtime and it never runs an ",
    "lede2a": " — a ",
    "lede2b": " is a row in ",
    "lede3": ", not a column in your schema.",
    "fieldLabel": "field",
    "paletteTitle": "field types",
    "paletteHint": "Click to add one, or drag it into the list to place it exactly.",
    "stringIndexSuffix": ", indexed on a 766-character prefix",
    "newModelLabel": "new model",
    "modelIdLabel": "model #{id}",
    "uncommitted": "uncommitted",
    "modelNameLabel": "model name",
    "noFieldsYet": "No fields yet. A model with no fields is perfectly legal — it just stores nothing but ids and timestamps until you add one.",
    "lockNoteP1": "The greyed rows are already in ",
    "lockNoteP2": ". ",
    "lockNoteP3": " is get-or-create — it can ",
    "lockNoteAdd": "add",
    "lockNoteP4": " fields to this model and nothing else. Changing one is a different call each time: ",
    "lockNoteP5": ", ",
    "lockNoteP6": ", ",
    "lockNoteP7": " and ",
    "lockNoteP8": " are separate operations, every one of them a migration that runs over live data rather than an edit that lands instantly. They get their own section, with the daemons visible while they drain.",
    "newModelButton": "new model",
    "committedTitle": "Committed to the registry",
    "reopenButton": "add fields",
    "indexedTag": "indexed",
    "filterableTag": "filterable",
    "pendingTag": "filterable · not indexed yet",
    "jsonOnlyTag": "JSON only",
    "noFieldsOnModel": "No fields on this model.",
    "orderNote1": "Fields are listed in id order, which is the only order there is — ",
    "orderNote2": " has no sort column. Whatever order you dragged them into was an argument to one call, and it did not survive the insert.",
    "asideBold": "Filterable is intent, not an index.",
    "asideRest": " It sets one boolean in the registry. No page is provisioned and no slot is reserved — that is a daemon's job, and until it happens a filter on the field is rejected rather than answered slowly.",
    "sideCodeTitle": "what your application would call",
    "statusMoved": "{field} moved to position {to} of {total}.",
    "fieldName": "name of field {index}",
    "declaredType": "declared type of {field}",
    "moveUp": "move {field} up",
    "moveDown": "move {field} down",
    "remove": "remove {field}",
    "inRegistryTag": "in the registry",
    "commit": {
      "insertedModel": "Inserted model #{id}.",
      "modelExisted": "Model #{id} already existed — returned unchanged.",
      "insertedFields": "Inserted {names}.",
      "fieldsExistedSingular": "{names} already existed and was left exactly as stored — an existing field's type and filterability are never reconciled against the arguments.",
      "fieldsExistedPlural": "{names} already existed and were left exactly as stored — an existing field's type and filterability are never reconciled against the arguments.",
      "versionBumped": "stardust_schema_version bumped to {version}.",
      "versionNotBumped": "Nothing was inserted, so stardust_schema_version was not bumped."
    }
  },
  "tableInspector": {
    "eyebrow": "section b",
    "title": "What that became in MySQL",
    "lede1": "Same names, same columns, same nullability as the schema ",
    "lede2": " creates — open the ",
    "lede3a": " on any panel and check. What you defined above is in the ",
    "lede3b": " tables. Everything else is empty, and stays empty until something specific makes it otherwise.",
    "registryLabel": "registry",
    "beat1Title": "Metadata and storage are different tables",
    "beat1Body1": "The registry describes your models; the data plane holds your data. Defining a field wrote to the first and not the second, which is why ",
    "beat1Body2": " has a row saying ",
    "beat1Body3": " while ",
    "beat1Body4": " has nothing at all. The gap between those two is not a bug being fixed later — it is a promise waiting for a daemon to keep it.",
    "beat2TitleSuffix": " is keyed by name",
    "beat2Body1": "Not by field id. That one encoding choice is why renaming a field cannot be an ",
    "beat2Body2": " on a registry row: every stored payload in the model is on the old key, and all of them have to be rewritten. Worth noticing now, because it is what makes renaming a field under load interesting later."
  },
  "worldInspector": {
    "tabsGroupLabel": "table groups",
    "groups": {
      "registry": {
        "label": "registry",
        "blurb": "Metadata. What models and fields exist, which slot on which page each filterable field holds. Defining a field writes here and nowhere else."
      },
      "data": {
        "label": "data plane",
        "blurb": "Storage. The complete JSON payload of every entry, plus the extension pages that mirror the filterable fields into typed, indexed columns."
      },
      "ops": {
        "label": "operational",
        "blurb": "The tables the daemons coordinate through. No broker anywhere — every one of the four reads and writes these and nothing else."
      }
    },
    "models": {
      "note": "one row per model",
      "about": "One row per model, unique per tenant by name. There is no updated_at — which is why renaming a model is a single UPDATE with nothing to wait for, unlike renaming a field.",
      "empty": "No models yet. This table is written by createModel()."
    },
    "fields": {
      "note": "declared_type + is_filterable",
      "about": "A field is a row here, not a column anywhere. is_filterable is intent — it says you would like to filter on this field, and creates nothing. previous_name and deleted_at are both drain markers: non-null means a migration is running right now.",
      "empty": "No fields yet. is_filterable here is intent only — it does not create a slot."
    },
    "pages": {
      "note": "one row per extension table",
      "aboutBefore": "One row per ",
      "aboutAfter": " table. Notice what is not here: which of a page's slot columns carry an index. That is never stored — the engine asks ",
      "aboutEnd": " when it needs to know.",
      "empty": "No pages. bootstrap() provisions none — a page appears when something needs slot capacity, and not before."
    },
    "slots": {
      "note": "one row per slot column per page",
      "about": "The inventory: one row per slot column per page, seeded free and claimed from there. status is a closed five-state ENUM, and a partial unique index enforces that a field holds at most one live slot — the database refuses the alternative rather than trusting the code.",
      "empty": "No slots, because there are no pages to carve them out of."
    },
    "entries": {
      "note": "system of record · always complete",
      "aboutBefore": "Every write lands here in full, first, whatever the index situation is. The ",
      "aboutMid": " JSON is keyed by field ",
      "aboutAfter": " — which is why renaming a field is a rewrite of every row in the model rather than a registry update.",
      "empty": "No entries yet. Every write lands here first, in full, whether or not any field is indexed."
    },
    "absentPage": {
      "tag": "not provisioned",
      "body1": "There is no extension page to show, and there will not be one until something asks for slot capacity. The DDL below is what a provisioner runs when that happens: four columns of each type family — ",
      "body2": ", ",
      "body3": " and ",
      "body4": " — and an index on every one of them. A page carries exactly what it indexes, because a slot column without an index is one no filterable field is allowed to occupy; the spare three per family are the headroom that lets the next few promotions skip the daemons entirely. The 25/15/10/10 layout is the ceiling one family can grow to on a page, not what it starts with.",
      "ddlTitle": "what a provisioner would run"
    },
    "version": {
      "note": "singleton · id = 1",
      "about": "One row, forever. Field metadata changes bump the version, so a cached schema snapshot can tell in one cheap read that it is stale instead of re-reading the registry on every request.",
      "empty": "Unreachable — the singleton is seeded at bootstrap."
    },
    "advisory": {
      "note": "singleton · id = 1",
      "about": "A second singleton, one advisory-sample due time for the whole deployment rather than one per process. NULL means never scheduled — the first daemon to see it picks a random moment inside the interval, so a fleet started in lockstep does not sample in a stampede.",
      "empty": "Unreachable — the singleton is seeded at bootstrap."
    },
    "syncQueue": {
      "noteEmpty": "backfill debt",
      "noteCount": "{count} rows of debt",
      "about": "Deliberately tiny: an id, an entry id, a timestamp. A row lands here when a write could not be mirrored into a slot, and the write still succeeds — indexing being behind is never a reason to refuse data.",
      "empty": "Empty. A row lands here when a write could not be mirrored into a slot — the write still succeeds."
    },
    "checkpoints": {
      "note": "resumable cursors",
      "about": "Where a long migration has got to. job_name says which lifecycle the row belongs to and last_processed_id says how far it drained, so a worker that dies mid-backfill is resumed rather than restarted.",
      "empty": "Empty. One row appears per running retype, rename or delete, and is removed when it lands."
    },
    "importJobs": {
      "note": "async bulk ingest",
      "about": "Submitted work, claimed by whichever worker gets there first. The manifest is written chunk by chunk rather than at the end, because it is also the resume point.",
      "empty": "No import jobs submitted."
    },
    "exportJobs": {
      "note": "async exports",
      "about": "A separate table with its own columns and its own auto-increment — import job 1 and export job 1 are different rows in different tables. There is no model_id column: it is stamped into the filter JSON alongside the consumer's own filter tree.",
      "empty": "No export jobs submitted."
    },
    "dlq": {
      "note": "poison pills",
      "about": "Where a row goes when it cannot be processed and retrying will not help. It has no foreign key to entry_data on purpose — one of the reasons is missing_entry_data, which only means anything if the record can outlive what produced it.",
      "empty": "Empty, which is the state you want. Rows here outlive their source entry on purpose."
    }
  },
  "pageTable": {
    "note": "{claimed} claimed of {total} indexed",
    "indexedTooltip": "indexed on this page",
    "about": "The mirror. A filterable field's value is copied out of the JSON payload into whichever slot column the reserver gave it, so a filter can read a real index instead of walking every document. Every column here carries an index — a page is created with exactly the columns it indexes, because one without an index is a column no field is allowed to occupy. The ones no field has claimed yet are the headroom the next promotion of that type will take.",
    "showingAll": "showing all {count}",
    "showAll": "show all {count} columns",
    "empty": "No mirrored rows yet. A row appears here when an entry is written to a model with at least one field holding a slot on this page."
  },
  "tableView": {
    "ddl": "DDL",
    "hideDdl": "hide DDL",
    "truncated": "showing the {shown} most recent of {total} rows",
    "truncatedNote": "{count} older rows are in the table and not drawn. That is this page's limit, not the database's — a browser will not render tens of thousands of cells, and MySQL does not care."
  },
  "entryWriter": {
    "eyebrow": "section c",
    "title": "Write entries",
    "lede1a": "The ",
    "lede1b": " lands in ",
    "payloadLabel": "payload",
    "lede2": " in full, first, whatever the index situation is. Mirroring a value out into a typed slot column is a separate step, and when it cannot happen the entry is queued rather than refused — which is why every ghost below stops short of the wall. Nothing has provisioned a page yet.",
    "fields": "fields (JSON)",
    "flightJsonOnly": "{name} — JSON only",
    "flightUnknownKey": "{name} — unknown key, stored verbatim",
    "blockedTitle": "no models",
    "blockedTag": "nothing to write into",
    "blockedBody1": "An entry belongs to a model, so there is nothing to compose until one exists. Define one in ",
    "blockedLink": "section A",
    "blockedBody2": " and this form builds itself from the fields you gave it.",
    "payloadTitle": "payload",
    "modelSelectLabel": "model",
    "pickOne": "pick one",
    "hintPickModel1": "Pick a model and its fields appear here, in registry order — which is id order, because ",
    "hintPickModel2": " has no sort column.",
    "hintNoFields": "This model has no fields. That is legal: every key you add below will be an unknown key, and unknown keys are stored verbatim.",
    "addKeyButton": "+ add a key this model does not have",
    "clearButton": "clear",
    "sideCodeTitle": "the call this is",
    "asideHeading": "Coercion touches the slot, not the payload",
    "asideBody1P1": "The chip beside a value is a preview of what a slot column ",
    "asideBody1Would": "would",
    "asideBody1P2": " hold. ",
    "asideBody1P3": " stores what you actually sent — so an ",
    "asideBody1P4": " field given the text ",
    "asideBody1P5": " keeps the string in JSON and would put the integer in the slot. The two are allowed to disagree, and the JSON is the one that is the system of record.",
    "asideBody2": "Nothing is coerced yet, because no slot exists to coerce for. The preview is here so the rule is visible before the daemons make it load-bearing.",
    "verdictIdlePrefix": "Run ",
    "verdictIdleSuffix": " to see what the payload split into.",
    "enqueuedTag": "enqueued for backfill",
    "nothingQueuedTag": "nothing queued",
    "status": {
      "mirrored": "mirrored into a slot",
      "waiting": "filterable, waiting on a slot",
      "jsonOnly": "JSON only",
      "unknown": "unknown keys",
      "queued": "{name} — queued for backfill"
    },
    "bucketEmptyNoPage": "None. No page is provisioned, so there is no slot column to mirror into.",
    "bucketEmptyNoFilterable": "None. Nothing in this payload is a filterable field.",
    "bucketEmptyGeneric": "None.",
    "bucketNoteQueue": "One row went into stardust_sync_queue for the whole entry — not one per field. The queue is {depth} deep.",
    "bucketNoteJsonOnly": "These never queue. Having no slot is their steady state, not a delay — so there is nothing for a daemon to fix.",
    "bucketNoteUnknown": "Not in stardust_fields for this model, stored verbatim, and readable back exactly as sent.",
    "mirrorNote": "system of record · always complete",
    "mirrorAbout1": "Every write lands here in full, before anything else is attempted. The ",
    "mirrorAbout2": " JSON is keyed by field ",
    "mirrorAbout3": ", and holds unknown keys and non-filterable fields exactly as they were sent. ",
    "mirrorAboutDeleteSoft": "Delete is soft",
    "mirrorAbout4": " — it stamps ",
    "mirrorAbout5": " and stops, keeping any slot values, because nothing can reach them without joining through a live row. Pressing it twice is not an error: the second call returns ",
    "mirrorAbout6": " and logs nothing, where an update to the same row would throw.",
    "mirrorEmpty": "No entries yet. Compose a payload above and press write() — it will land here whether or not any field is indexed.",
    "deleteTrue": "deleteEntry({tenantId}, {entryId}) returned true — deleted_at is stamped, the slot values are kept, and one entry_deleted line was logged.",
    "deleteFalse": "deleteEntry({tenantId}, {entryId}) returned false. Nothing transitioned, nothing was logged, and the original timestamp is untouched — a repeat delete has already achieved what you asked for. An update to the same row would have thrown instead.",
    "wallLabel": "mirror the filterable fields",
    "absentTag": "not provisioned",
    "absentBody1a": "There is nowhere for a value to be mirrored ",
    "absentBody1To": "to",
    "absentBody1b": ". Marking a field filterable wrote ",
    "absentBody1c": " to the registry and nothing else — no page, no slot, no index. So every filterable field in the payload above lands in ",
    "absentBody1d": " instead, the write succeeds anyway, and the value is safe in the JSON until something catches up.",
    "absentNote": "A page is a set of typed columns, every one of them indexed, and it appears when a daemon decides capacity is needed. That is the next section's job, and this is the debt it will be draining.",
    "ddlTitle": "what a provisioner would run",
    "seedTag": "one transaction per chunk",
    "seedBody1a": "{seedCount} rows in one call. The engine chunks at {chunkSize} and opens a transaction per chunk, so this commits {chunks} of them — {chunkSize}, then {remainder} — and logs one line each. It does ",
    "seedBody1Not": "not",
    "seedBody1b": " log per entry: the bulk path reports at chunk level, which is why the stream below gains {chunks} lines rather than {seedCount}.",
    "seedBody2a": "Above {threshold} entities the synchronous call is refused outright and you use ",
    "seedBody2b": ", which queues a job instead. Every number on this page is a real array length in the simulated tables — nothing here is scaled or estimated.",
    "seedButton": "seed {count} rows",
    "logTitle": "what the write path logged",
    "logEmpty": "Nothing yet. Defining a model logs a message rather than an event, so this stays empty until the first write.",
    "payloadRow": {
      "unknownKeyLabel": "unknown payload key",
      "registeredFieldTitle": "a field of this model",
      "notInRegistry": "not in the registry",
      "noSlotTag": "no slot",
      "noSlotTooltip": "a filter on this would be rejected",
      "backfillingTag": "backfilling",
      "indexedTag": "indexed",
      "valuePlaceholder": "leave empty to omit the key",
      "valueLabel": "value for {field}",
      "coercePrefix": "would coerce to",
      "removeKeyLabel": "remove key {field}",
      "removeKeyTitle": "remove this key",
      "lockedRemoveTitle": "a field of this model — edit it in section A"
    }
  },
  "daemonRoom": {
    "eyebrow": "section d",
    "title": "four daemons, one stream",
    "heading": "The daemon control room",
    "lede1": "Four processes on four different poll periods, none of which has ever heard of the others. Stop one and watch what stops with it — then promote a field with the ",
    "lede2": " down and see the ",
    "lede3": " and the index disagree, out loud, until you let it finish.",
    "watcherLabel": "Watcher",
    "registryLabel": "registry",
    "beat1Title": "Provisioning capacity is not claiming it",
    "beat1Body": "The Watcher adds a page and stops. The field that caused it is still unmapped when that tick ends, and a different daemon claims the slot on its own schedule. That gap is the design, not a lag — and it is why the two of them can be started, stopped and scaled independently.",
    "beat2Title": "Every arrow is a connection to MySQL",
    "beat2Body1": "There is no broker, no queue server and no daemon-to-daemon call anywhere in this design. The Liberator handing a slot back as ",
    "beat2Body2": " is what stops the Watcher provisioning another page, and neither knows the other is running.",
    "watcherRole": "Provisions indexed pages before capacity runs out — and the moment a field is waiting on a family with nothing claimable, whatever the threshold says.",
    "reconcilerRole": "Drains work sources round-robin under SKIP LOCKED. Horizontal scale is literally more processes; three of them run here.",
    "liberatorRole": "Sweeps a tombstoned slot's residue out chunk by chunk, then hands the column back as free so it can be reused.",
    "chroniclerRole": "Claims export jobs and streams a CSV or JSON artifact straight to disk, cursor-paginated and never buffered.",
    "caveatBold": "“Capacity” means an indexed free slot of the field's own type family",
    "caveatBody1": " — not a free slot. A page is created with exactly the columns it indexes, four of every type family, because a column without an index is one no filterable field may occupy: a page of sixty columns indexing the two that were asked for was fifty-eight columns of nothing. So the first promotion on a fresh schema still takes the slow path — there is no page at all, the reservation is deferred, the Watcher wakes and the Reconciler picks it up — but the next three of that type take the fast path, reserved inside the transaction ",
    "caveatBody2": " itself runs, with the Watcher never stirring. Promote one field below, let the chain finish, then promote another of the same type and watch none of it happen. The fifth is slow again: the family's headroom is spent, and a page is a page.",
    "logNote": "NDJSON · stdout",
    "logEmpty": "Nothing has polled yet. Press run on the clock above, or step it one tick at a time — the daemons emit only when they actually do something, so an idle tick is silent.",
    "startButton": "start",
    "stopButton": "stop",
    "pollsEvery": "polls every {period} ticks",
    "stoppedNotPolling": "stopped — not polling",
    "nextIn": "next in {ticks}",
    "notPolledYet": "has not polled yet",
    "freeSlots": "free slots",
    "ofTotal": "of {total}",
    "noPageProvisioned": "no page provisioned — bootstrap creates none, and one appears only when something needs it",
    "slotsAcrossPagesOne": "{total} slot across {pages} page",
    "slotsAcrossPagesMany": "{total} slots across {pages} pages",
    "pendingDemand": "pending demand",
    "noDemand": "none — every filterable field holds a slot",
    "demandChip": "{waiting} waiting · {claimable} claimable",
    "syncQueuePending": "stardust_sync_queue · {count} pending",
    "workerIdle": "idle",
    "claimCapacityWait": " · capacity_wait",
    "claimRolledBack": " · claimed {claimed}, rolled back, reserved a slot",
    "claimBare": " · {claimed}",
    "claimRows": " · {claimed} rows, ids {firstId}–{lastId}",
    "reconcilerFootnote1": "Nothing assigns work to a worker. Each claims the next rows nobody else is holding — that is all ",
    "reconcilerFootnote2": " does, and it is the whole coordination mechanism. A chunk is 500 rows, so one 600-row seed keeps two workers busy and leaves the third with nothing to take.",
    "liberatorEmpty": "nothing tombstoned — demote a field below and a column full of values nobody will read again appears here",
    "liberatorFootnote1": "The sweep never joins ",
    "liberatorFootnote2": ". It keys on the page, the column and a cursor, which is what lets it reclaim a slot whose field row is already gone.",
    "exportJobsCount": "stardust_export_jobs · {count} rows",
    "chroniclerEmpty": "no jobs to claim",
    "chroniclerFootnote": "Its poll period and stop button are real and it is genuinely being asked to run — there is simply nothing submitting exports yet. A Chronicler with an empty job table claims nothing and emits nothing, which is exactly what this card is showing. Submitting one is the operations section, alongside bulk import and dead-letter replay.",
    "activity": {
      "watcherWaiting": "{waiters} waiting, capacity available — nothing to provision",
      "watcherHealthy": "capacity healthy, no demand",
      "watcherProvisioned": "provisioned page {pageId} ({trigger}), indexed {indexed}",
      "liberatorReclaimedOne": "nullified {nullified}, returned {reclaimed} slot to free",
      "liberatorReclaimedMany": "nullified {nullified}, returned {reclaimed} slots to free",
      "liberatorSweptOne": "nullified {nullified} across {batchSize} tombstoned slot",
      "liberatorSweptMany": "nullified {nullified} across {batchSize} tombstoned slots",
      "reconcilerIdle": "nothing to claim",
      "reconcilerBusy": "{busy} of {total} workers claimed work"
    }
  },
  "sharedState": {
    "groupLabel": "sole coordination point — no broker, no daemon-to-daemon RPC",
    "syncQueueRowOne": "row pending",
    "syncQueueRowMany": "rows pending",
    "syncQueueNote": "backfill debt — one row per write that outran its index",
    "pageOne": "page",
    "pageMany": "pages",
    "freeSlotsAcrossOne": "{count} free slot across them",
    "freeSlotsAcrossMany": "{count} free slots across them",
    "tombstonedUnit": "tombstoned",
    "tombstonedNote": "columns still holding residue nobody will read",
    "runningUnit": "running",
    "runningNote": "lifecycles a Reconciler worker can claim",
    "quarantinedUnit": "quarantined",
    "quarantinedNote": "rows that cannot succeed, kept rather than dropped"
  },
  "fieldIndexReadout": {
    "empty": "Define a model in section A first. This panel reads the registry, and there is nothing in it yet.",
    "headTag": "registry intent vs. live index",
    "promote": "promote",
    "demote": "demote",
    "checkpointInFlightTitle": "A retype is already in flight for this field. The engine refuses an overlapping lifecycle rather than queueing it.",
    "demoteTitle": "demoteFieldFromFilterable() — registry-only, and the slot is tombstoned for the Liberator",
    "promoteTitle": "promoteFieldToFilterable() — returns immediately; a daemon finishes it",
    "filterableNote": "registry intent — set the moment the call returned",
    "indexedNoteTrue": "a filter reads a real index right now",
    "indexedNoteFalse": "no live slot a filter could use yet",
    "slotLabel": "slot",
    "noneReserved": "none reserved",
    "verdictLive": "a filter on this field is compiled against the slot column",
    "verdictBuilding": "a filter is rejected — FieldNotFilterableException, because the index is only half built",
    "verdictNoSlot": "a filter is rejected — FieldNotFilterableException, no slot exists to read",
    "verdictJsonOnly": "JSON-only by design. A filter is rejected until you promote it.",
    "checkpointTotalNote": "of {total} rows in entry_data for this model — the table stores no total, so this is counted from the partition being drained"
  },
  "clockBar": {
    "run": "run",
    "pause": "pause",
    "step": "step",
    "reducedTitle": "Reduced motion is on, so the clock does not run itself. Use step.",
    "stepTitle": "Advance exactly one tick",
    "tickLabel": "tick",
    "speedGroupLabel": "clock speed",
    "daemonsGroupLabel": "daemons",
    "daemonStopped": "{name} is stopped — it will not run on its poll period",
    "daemonPolls": "{name} polls every {period} ticks",
    "resetWorld": "reset world"
  },
  "nowLine": {
    "label": "now",
    "idle": "nothing in flight",
    "building": "{fieldName} building {cursor}/{total}",
    "rewriting": "{previousName} → {currentName} rewriting {cursor}/{total}",
    "purging": "{label} purging {cursor}/{total}",
    "deleting": "{label} deleting · {total} rows left",
    "queuedRow": "{count} row queued for a slot",
    "queuedRows": "{count} rows queued for a slot",
    "allStopped": "all daemons stopped",
    "someStopped": "{names} stopped"
  },
  "shareLink": {
    "trigger": "share",
    "triggerTitle": "A link that rebuilds this world on someone else's screen",
    "groupLabel": "share this world",
    "eyebrow": "share",
    "dismiss": "dismiss",
    "emptyNote": "There is nothing to share yet. Define a model, load a scenario, or start the guided tour, and this link will rebuild it.",
    "tooBigNote": "This world is past what a link can carry — too many models, or too many fields on one of them. Everything still works here; there is just no URL short enough to rebuild it on someone else's screen.",
    "urlLabel": "link to this world",
    "copy": "copy",
    "copied": "copied",
    "carriesPrefix": "Carries ",
    "carriesSuffix": ". The link is a recipe, not a copy of the database — opening it rebuilds this world by replaying the same actions, so it stays short enough to paste anywhere.",
    "caveatSingular": "Rows written by hand cannot travel: {names} arrives with its schema and no rows. Seeded rows are reproduced exactly, because the seed is deterministic.",
    "caveatPlural": "Rows written by hand cannot travel: {names} arrive with their schema and no rows. Seeded rows are reproduced exactly, because the seed is deterministic.",
    "sharedEyebrow": "shared link",
    "sharedPrefix": "Someone shared a world with you: ",
    "sharedSuffix": ". You already have one open, so nothing has changed yet.",
    "loadButton": "load it — this replaces your world"
  },
  "queryBuilder": {
    "eyebrow": "section e",
    "heading": "Query it",
    "lede1": "Build a filter against the schema you defined, over the rows you wrote, using the index the ",
    "lede2": " built. Then break it on purpose: filter a field nobody ",
    "lede3": ", or hand the decoder JSON it will not take, and read what comes back instead of rows.",
    "daemonLabel": "daemons",
    "promotedLabel": "promoted",
    "beat1Title": "Two queries, however many conditions",
    "beat1Body1": "One bounded probe over the joined pages returns ",
    "beat1Body2": " ids; one fetch materialises them. Stack five conditions across three pages and it is still two queries and still one row per entry per page — no fan-out, because the values live in columns rather than in rows.",
    "beat2Title": "A filter reads the slot, not the JSON",
    "beat2Body1": " is the system of record and it is not what a filter touches. A row the backfill has not reached has no row on the page at all, so it matches nothing — ",
    "beat2Body2": " included, because that still needs a row to exist. Stop the Reconciler mid-drain and you can watch the two halves of the model answer differently.",
    "emptyBody": "Define a model in section A first, and write a few rows in section C. This section filters what those two produced; with an empty registry there is nothing to build a condition against.",
    "requestLabel": "request",
    "modelLabel": "model",
    "pickOne": "pick one",
    "hintPickModel": "Pick a model to filter.",
    "matchAllBefore": "No conditions. The envelope omits its ",
    "matchAllAfter": " key entirely, which is the match-all signal — every non-deleted row of this model, cursor-paginated.",
    "addFieldAriaLabel": "field to add a condition on",
    "notIndexedSuffix": " (not indexed)",
    "addConditionButton": "+ condition",
    "conditionsHint1": "Conditions join with ",
    "conditionsHint2": " by default. Use ",
    "conditionsHint3": " or ",
    "conditionsHint4": " on a row to group it — that is what moves the compiler onto the EXISTS strategy.",
    "sortLabel": "sort",
    "ascending": "ascending",
    "descending": "descending",
    "sortDirectionAriaLabel": "sort direction",
    "slotColumnSuffix": " (slot column)",
    "pageSizeLabel": "pageSize",
    "clearButton": "clear",
    "runButton": "run",
    "sortNoteBold": "not part of the wire format",
    "sortNote1": "Sorting is ",
    "sortNote2": " — it is a parameter alongside the filter, which is why it is up here and not in the JSON below. The two intrinsic targets stay index-ordered; a sort on a field is an honest filesort over the whole filtered set.",
    "apiLogTitle": "what the API logged",
    "apiLogEmpty1": "Nothing yet. A search logs one ",
    "apiLogEmpty2": " line; a refusal logs ",
    "apiLogEmpty3": " with the reason instead, and never both."
  },
  "filterTree": {
    "switchToOrTitle": "Switch to OR. An OR anywhere in the tree moves the whole query to the EXISTS strategy.",
    "switchToAndTitle": "Switch back to AND. A pure-AND tree compiles to one INNER JOIN per page.",
    "andNote": "every condition must hold — one join per page, no fan-out",
    "orNote": "any condition may hold — compiles to EXISTS subqueries",
    "notNote": "negated — and a negation over a NULL slot is still not a match, because SQL says UNKNOWN rather than true",
    "fieldAriaLabel": "field",
    "operatorAriaLabel": "operator",
    "noValue1": "no value — ",
    "noValue2": " carries none, and sending one is ",
    "lowerBoundAriaLabel": "lower bound",
    "and": "and",
    "upperBoundAriaLabel": "upper bound",
    "valueAriaLabel": "value",
    "valuePlaceholder": "value",
    "setPlaceholder": "comma, separated, values",
    "indexedTag": "indexed",
    "backfillingTag": "backfilling",
    "noSlotTag": "no slot",
    "wrapOrTitle": "Wrap this in an OR group",
    "negateTitle": "Negate this — NOT (…)",
    "removeTitle": "Remove",
    "removeAriaLabel": "remove this condition"
  },
  "queryPlan": {
    "planLabel": "the plan",
    "strategyTag": "{strategy} strategy",
    "queriesTag": "2 queries",
    "joinsExplanation1": "A pure-",
    "joinsExplanation2": " tree, so the compiler joins one ",
    "joinsExplanation3": " per distinct page and ANDs the predicates in the outer ",
    "joinsExplanation4": ". That is the shape the composite index was built for.",
    "existsExplanation1": "The tree contains an ",
    "existsExplanation2": " or a ",
    "existsExplanation3": ", so every leaf becomes an ",
    "existsExplanation4": " subquery composed with native SQL. Each one still hits the composite index on its own page.",
    "orderedByPrefix": "Ordered by ",
    "fieldSortSuffix": " — a slot column on a joined page, which means a real filesort over the whole filtered set. Bounded materialisation, unbounded discovery: the two intrinsic targets stay index-ordered and this one does not.",
    "probeTitle": "query 1 · bounded probe",
    "bindingsPrefix": "bindings: ",
    "fetchTitle": "query 2 · materialise the page",
    "probeIdsOne": "{count} id came back from the probe",
    "probeIdsMany": "{count} ids came back from the probe",
    "hasMoreSuffix": " — one more than the page, which is the entire has-more protocol.",
    "noMoreSuffix": ".",
    "callTitle": "the call"
  },
  "resultPanel": {
    "nothingRunTag": "nothing has run yet",
    "nothingRunBody1": "Build a condition and press ",
    "nothingRunRun": "run",
    "nothingRunBody2": ". With no conditions at all the filter key is omitted, which is match-all — a perfectly good query, and the one every listing screen starts as.",
    "darkHeader": "result · nothing",
    "darkTag": "model not visible to reads",
    "darkBody1": "Not an error — ",
    "darkNothing": "nothing",
    "darkBody2": ". This model is being deleted, and a read against it returns exactly what a read against a model id that never existed returns. There is no exception to catch and no rejection to log, which is deliberate: severance is total, and a consumer polling a deleted model should see it empty rather than learn that it once existed.",
    "refusedHeader": "result · refused at pre-flight",
    "refusedHint1": "Nothing was executed. The refusal came from the pre-flight pipeline before any SQL was built, which is why there is no plan below — and it is logged as ",
    "refusedHint2": " with ",
    "refusedHint3": " so an operator can count it.",
    "decoderRejectedHeader": "result · rejected by the decoder",
    "decoderRejectedBody": "The envelope never became a filter, so nothing reached the registry. The pointer above the editor names the node.",
    "resultPageHeader": "result · page {page}",
    "rowsOne": "{count} row",
    "rowsMany": "{count} rows",
    "matchedTag": "{count} matched",
    "probeHint1": "The probe considered {candidates} rows of this model, {matched} matched, and {materialised} were materialised. ",
    "probeHintBold": "The engine never reports that middle number",
    "probeHint2": " — there is no count query and no total; it is on screen here because the simulation can see the whole table and a real read deliberately cannot.",
    "mirrorNote": "the payload, verbatim",
    "emptyResult": "No rows matched. That is an answer, not a failure — and it is a different thing from the refusals above, which is why this panel looks different when one happens.",
    "prevButton": "← previous",
    "nextButton": "next →",
    "hasMoreNote": "the probe asked for pageSize + 1 rows and got them, so there is another page",
    "lastPageNote": "the probe came back short of pageSize + 1, so this is the last page",
    "cursorLabel": "nextCursor",
    "v1Tag": "v1",
    "v1Note1": " anchor id ",
    "v1Note2": " — the format that predates sorting, still emitted for an unsorted read so old tokens keep working",
    "v2Tag": "v2",
    "v2Note1": " anchor id ",
    "v2Note2": ", ordering ",
    "v2Note3": " — the value itself is ",
    "v2NoteNot": "not",
    "v2Note4": " in the token; it is resolved from the anchor row at query time, which keeps the token constant-size"
  },
  "wirePane": {
    "viewLabel": "queryfilter · {view}",
    "wireFormatView": "wire format",
    "astView": "decoded AST",
    "jsonButton": "JSON",
    "astButton": "PHP AST",
    "rederiveButton": "re-derive",
    "rederiveTitle": "Discard the hand-edited text and re-derive it from the builder",
    "astTitle": "what JsonFilterDecoder::decode() returns",
    "astHint1": "The tree the decoder produces, before pre-flight touches it. Each ",
    "astHint2": " here carries only the two names the wire format sent; resolution adds the ",
    "astHint3": ", ",
    "astHint4": " and descriptor, which is the step that can fail with ",
    "astHint5": ".",
    "wireAriaLabel": "QueryFilter wire format",
    "decodesNote1": "Decodes. The engine's ",
    "decodesNote2": " accepts this envelope, and running the query decodes this exact text — not the builder above it.",
    "wholeEnvelope": "(whole envelope)",
    "filterKeyHint1": "The ",
    "filterKeyHint2": " key is ",
    "filterKeyHintOmitted": "omitted",
    "filterKeyHint3": " for match-all, never set to ",
    "filterKeyHint4": " — sending ",
    "filterKeyHint5": " is the one shape the decoder singles out, on the grounds that a caller who meant everything had a way to say so."
  },
  "scenarioPicker": {
    "groupLabel": "scenario presets",
    "label": "scenario",
    "replaceWorld": "replace world?",
    "discardWarning": "This discards the world you have now",
    "parkedEyebrow": "parked",
    "dismiss": "dismiss",
    "next": "Next:",
    "goTo": "go to {target} →"
  },
  "tourPanel": {
    "toggleGroupLabel": "playground mode",
    "guided": "guided",
    "sandbox": "sandbox",
    "replaceWorld": "replace world?",
    "discardWarning": "This discards the world you have now",
    "guidedHint": "A scripted walk through the whole lifecycle, one step at a time",
    "sandboxHint": "Every control unlocked, nothing scripted",
    "regionLabel": "Guided tour",
    "progress": "guided · {index} of {total}",
    "exit": "exit",
    "where": "in {section}",
    "next": "next",
    "toSandbox": "to the sandbox",
    "restart": "restart"
  },
  "narrationFeed": {
    "history": "what has happened",
    "lastCount": "last {count}",
    "footer": "The complete stream, unabridged, is the event log in the daemon section.",
    "liveRegionLabel": "What just changed",
    "dismiss": "Dismiss: {headline}",
    "goTo": "go to {target} →",
    "hide": "hide",
    "toggle": "what has happened · {count}"
  },
  "eventLog": {
    "title": "event stream",
    "note": "NDJSON · one line per event",
    "truncated": "the world retains the last {limit} lines — older ones have scrolled off, not been suppressed",
    "regionLabel": "Simulated engine event stream"
  },
  "schemaEvolver": {
    "eyebrow": "section f",
    "heading": "Change the schema while it is live",
    "lede1": "Renaming a field is not a registry update — it is a rewrite of every row in the model, because the ",
    "lede2": " is keyed by name. Stop the ",
    "lede3": " first and you can stand inside the migration and look around.",
    "payloadLabel": "payload",
    "reconcilerLabel": "Reconciler",
    "beat1Title": "A window, not an instant",
    "beat1Body1": "The registry flips the moment the call returns. Every payload written before that is still keyed by the old name, and stays that way until the backfill reaches it. The engine's job is not to hide that — it is to make every surface answer correctly ",
    "beat1During": "during",
    "beat1Body2": " it.",
    "beat2Title": "Reads bridge it; filters do not",
    "beat2Body1": "A read falls back from the new key to the old one, and a write is rewritten onto the new name before it is stored. A ",
    "beat2Filter": "filter",
    "beat2Body2": " naming the old field is refused outright. That asymmetry is deliberate: a refused filter loses nothing and says so immediately, and a mis-keyed write loses data silently.",
    "emptyBody": "Nothing to change yet. Define a model in section A and write some rows in section C — a rename over an empty model completes on the Reconciler's first tick, which is correct and shows you nothing.",
    "logTitle": "the registry's own lines",
    "logNote": "NDJSON · source=registry",
    "logEmpty": "Nothing has changed the schema yet. Rename, retype or delete a field above and the transition appears here — the drain that carries it out is in the daemon room's stream.",
    "noopBody1": "That call returned ",
    "noopBody2": " — nothing to do. The engine reports an unknown id, another tenant's, and a deletion already in flight the same way, which is what makes a repeated delete idempotent.",
    "modelTenantTag": "tenant {tenantId}",
    "purgingTag": "deleted_at set · purging",
    "rowsLeftToDestroy": "rows left to destroy",
    "fieldsStillVisible": "fields still visible to this panel",
    "modelPurgeTotalNote": "of the rows still in entry_data — this partition shrinks as it is walked, unlike every other drain on this page, because the chunks delete what they claim",
    "modelDarkBold": "The model has gone dark.",
    "modelDarkBody1": " A read returns nothing — not an error, nothing, exactly as for a model that never existed — and a write is ",
    "modelDarkRefused": "refused",
    "modelDarkBody2": " rather than having its keys stripped. That is the deliberate inversion of the field rule: a deleted field leaves a valid residual entry and a deleted model leaves nothing to preserve. The final chunk drops the model row and cascades every field row away with it.",
    "renameModelLabel": "rename the model",
    "renameModelHint": "One UPDATE. Complete on return — no checkpoint, no window, and deliberately no schema-version bump.",
    "renameModelFootnote1": "Watch what does ",
    "renameModelFootnoteNot": "not",
    "renameModelFootnote2": " happen: no row appears in ",
    "renameModelFootnote3": " and ",
    "renameModelFootnote4": " does not move. Nothing cached holds a model name, so there is nothing to invalidate. The one real consequence is a footgun the engine keeps on purpose — ",
    "renameModelFootnote5": " is get-or-create keyed on the name, so a seed script still naming the old model makes a second one.",
    "callTitle": "what this button calls",
    "deleteModelLabel": "delete this model",
    "deleteModelConfirm": "destroy {count} rows?",
    "deleteModelFootnoteBold1": "This is the only operation in the engine that physically removes ",
    "deleteModelFootnoteBold2": " rows, and there is no undelete.",
    "deleteModelFootnote1": " Severance is one transaction — the model and every field it owns are marked at once, which is what makes every existing field guard fire with no new conditions. The rows themselves go in chunks, along with their ",
    "deleteModelFootnote2": " entries, and the model row is dropped by the final chunk.",
    "previousNameTag": "previous_name = {name}",
    "renameFieldLabel": "rename the field",
    "renameInFlightHint": "A rename is already draining for this field. The engine refuses an overlapping lifecycle rather than queueing it.",
    "retypeInFlightHint": "A retype is in flight. Its backfill locates values by name, so a rename underneath it would write NULL slots silently.",
    "renameReadyHint": "Returns as soon as the registry commits. The payload rewrite needs a running Reconciler.",
    "rewrittenTo": "rewritten to {name}",
    "stillStoredAs": "still stored as {name}",
    "renameTotalNote": "of {total} rows in entry_data for this model — backfill_checkpoints stores a cursor and a status and no total, so this is counted from the partition being drained",
    "renameWindowFootnote1": "A read right now returns every one of those {total} rows under ",
    "renameWindowFootnote2": ". The {stale} that are still stored as ",
    "renameWindowFootnote3": " resolve through the fallback — which is the only reason the window is survivable. A filter naming ",
    "renameWindowFootnote4": ", by contrast, is refused as an unknown field: as far as the registry is concerned it no longer exists.",
    "deleteFieldLabel": "delete {name}",
    "deleteFieldConfirm": "delete permanently?",
    "deleteFieldFootnote1": "Severs from every surface in one commit and purges the payloads asynchronously. Its name is ",
    "deleteFieldNotReusable": "not reusable",
    "deleteFieldFootnote2": " until the purge lands — ",
    "deleteFieldFootnote3": " is unconditional, so the dying row still holds it.",
    "inFlightTitle": "in flight",
    "whatRowCanCall": "what this row can call",
    "retypeToLabel": "retype to",
    "alreadyThisType": "Already this type.",
    "retypeRejectedTitle": "{from} → {to} is categorically refused: there is no defensible epoch convention to pick between seconds, milliseconds and a packed date.",
    "retypeAllowedTitle": "retypeField() — overwrites declared_type now and rewrites every value through the {from} → {to} cell of the matrix.",
    "retypeHint1": "The old slot is tombstoned and a replacement is reserved from the ",
    "retypeHintNew": "new",
    "retypeHint2": " family — so a retype across families needs the Watcher to provision a page it has no indexed column on yet. Values that will not convert are written ",
    "retypeHint3": " with an audited reason rather than rounded or truncated: ",
    "retypeHint4": " becoming an ",
    "retypeHint5": " is a ",
    "retypeHint6": ", not a ",
    "retypeHint7": ".",
    "purgingPayloadsStillCarrying": "payloads still carrying the key",
    "purgingTotalNote": "of {total} rows in entry_data for this model",
    "purgingFootnote1": "It is already gone from reads, filters, exports and ",
    "purgingFootnote2": " — and a write still sending the key has it silently ",
    "purgingFootnoteStripped": "stripped",
    "purgingFootnote3": " rather than refused, which is what bounds the purge. An unregistered key would otherwise be preserved verbatim, so a client that had not redeployed would keep writing the field back into rows the cursor had already passed. Look at ",
    "purgingFootnote4": " in the table inspector: the values are visibly still there.",
    "renameButton": "rename"
  }
}
