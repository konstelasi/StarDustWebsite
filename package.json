{
  "name": "stardust-website",
  "version": "0.1.0",
  "private": true,
  "description": "Landing page for StarDust — a MySQL & MariaDB-native Vertical Schema Partitioning engine for dynamic data models.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit",
    "verify:scenarios": "tsc -p tsconfig.scripts.json && node .scenario-build/scripts/verify-scenarios.js",
    "verify:narration": "tsc -p tsconfig.scripts.json && node .scenario-build/scripts/verify-narration.js",
    "verify:lifecycles": "tsc -p tsconfig.scripts.json && node .scenario-build/scripts/verify-lifecycles.js",
    "verify:tour": "tsc -p tsconfig.scripts.json && node .scenario-build/scripts/verify-tour.js",
    "verify:link": "tsc -p tsconfig.scripts.json && node .scenario-build/scripts/verify-link.js",
    "verify:contrast": "tsc -p tsconfig.scripts.json && node .scenario-build/scripts/verify-contrast.js",
    "verify:glossary": "tsc -p tsconfig.scripts.json && node .scenario-build/scripts/verify-glossary.js",
    "verify:messages": "tsc -p tsconfig.scripts.json && node .scenario-build/scripts/verify-messages.js",
    "deploy": "bash deploy.sh"
  },
  "dependencies": {
    "next": "15.5.4",
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "devDependencies": {
    "@types/node": "22.10.2",
    "@types/react": "19.0.2",
    "@types/react-dom": "19.0.2",
    "typescript": "5.7.2"
  }
}
